ansible_host_key_checking: False
ansible_become: yes
ansible_become_method: sudo
ansible_user: "{{ user_name if user_name is defined else 'admin' }}"
ansible_password: "{{ ask_password if ask_password is defined else lookup('vars',password_var) if password_var is defined else None }}"
cloud_type: "{{ cloud if cloud is defined else 'aws' }}"
file_type: "{{ files if files is defined else cloud_type }}"
bucket_name: "{{ bucket if bucket is defined else None }}"
access_key: "{{ key if key is defined else None }}"
secret_key: "{{ secret if secret is defined else None }}"
sw_dir: "{{ software if software is defined else '/opt/software' }}"
swap_dev: "{{ swap if swap is defined else None }}"
data_dev: "{{ data if data is defined else '/dev/sdb' }}"
root_dir: "{{ root if root is defined else '/opt' }}"
host_num: "{{ start if start is defined else '1' }}"
cloud_region: "{{ region if region is defined else 'us-east-1' }}"
os_strategy: "{{ os_type if os_type is defined else 'systemd' }}"
dns_key: "{{ dnskey if dnskey is defined else 'dynamicdns' }}"
dns_secret: "{{ dnssecret if dnssecret is defined else None }}"
dns_host: "{{ dnsserver if dnsserver is defined else '127.0.0.1' }}"
dns_zone: "{{ domain if domain is defined else None }}"
run_setup: "{{ install if install is defined else 'false'|bool }}"
